/* Exemplo de Programa na linguagem L */

int n;
string nome;
boolean naoTerminou <- TRUE;
final MAXITER<-10;
FINAL teste <- 2;

/* Programa Principal */

write("Digite seu nome: ");
readln(nome);
naoTerminou<-TRUE;
n<-0;
while (nome="Vinicius" || MAXITER>=11) 1+2>(MAXITER/2) * 1;

writeln("Ola' ",nome);
naoTerminou<-n<MAXITER;
endwhile